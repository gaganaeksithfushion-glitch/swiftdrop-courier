<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Camera photo scan සඳහා -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Gemini API call කිරීම සඳහා internet access අවශ්‍යයි -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- WhatsApp Message එකට Location Link එක Attach කිරීම සඳහා GPS Location -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- WhatsApp සහ අනෙකුත් බාහිර යෙදුම් බිල්ඩ් එකේදී හඳුනා ගැනීමට අත්‍යවශ්‍ය queries කොටස -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
        <package android:name="com.whatsapp" />
    </queries>

    <application
        android:label="swiftdrop_courier"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
