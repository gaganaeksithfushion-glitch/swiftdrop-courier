name: swiftdrop_courier
description: "A lightweight delivery management application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # පහතින් ඇත්තේ අපිට අලුතින් අවශ්‍ය packages දෙකයි
  image_picker: ^1.1.2
  google_generative_ai: ^0.4.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
