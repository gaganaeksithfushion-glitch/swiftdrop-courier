name: swiftdrop_courier
description: "A new Flutter project for ShiftDrop Courier."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  mobile_scanner: ^5.2.3
  url_launcher: ^6.3.2
  geolocator: ^10.1.1
  google_maps_flutter: ^2.18.0
  shared_preferences: ^2.5.5
  pdf: ^3.13.0
  printing: ^5.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  # assets හෝ images අවශ්‍ය නම් මෙහි එකතු කළ හැක
  # assets:
  #   - assets/
